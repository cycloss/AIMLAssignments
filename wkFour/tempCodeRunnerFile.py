for attr in attributes:
#     # selects the target attribute and output column
#     split = df[[attr, output]]
#     # for selected attribute split it into groups
#     grouped = split.groupby(attr)
#     # calculate total entropy of each group
#     for name, group in grouped:
#         print(name)
#         print(group)